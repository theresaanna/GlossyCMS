'use client'

import React from 'react'

const AdminLogo = () => {
  return (
    <span
      style={{
        fontFamily: "'Playwrite AT', cursive",
        fontStyle: 'italic',
        fontSize: '2.25rem',
        lineHeight: '2.5rem',
        color: 'var(--theme-text)',
      }}
    >
      Glossy
    </span>
  )
}

export default AdminLogo
